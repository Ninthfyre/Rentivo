# RentFlow

A premium tenant management web app for landlords and property managers.

## Run locally

Open the project folder and serve it with a static web server:

```bash
python -m http.server 8000
```

Then visit:

```text
http://localhost:8000
```

## Deploy to GitHub Pages

This project is configured as a static site and can be published directly from the main branch using GitHub Pages.
